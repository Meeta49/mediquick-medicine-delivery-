# mediquick-medicine-delivery-
MediQuick is a full-stack medicine delivery app built with Flutter (mobile), React.js (web), and Node.js/Express (backend). Features include medicine search, cart management, order placement, and stock tracking via 5 REST API endpoints connecting Flutter and React clients to a shared backend.
